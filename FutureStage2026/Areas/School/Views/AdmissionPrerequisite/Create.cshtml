@model FutureStage2026.Models.AdmissionPrerequisite

@{
    ViewData["Title"] = "Add Admission Prerequisite";
}

<div class="card shadow-sm border-0">

    <div class="card-header bg-white">

        <h3 class="mb-0 fw-bold">
            Add Admission Prerequisite
        </h3>

    </div>

    <div class="card-body">

        <form method="post">

            <div asp-validation-summary="ModelOnly"
                 class="alert alert-danger">
            </div>

            <div class="mb-3">

                <label asp-for="PrerequisiteTitle"
                       class="form-label fw-semibold">
                </label>

                <input asp-for="PrerequisiteTitle"
                       class="form-control" />

                <span asp-validation-for="PrerequisiteTitle"
                      class="text-danger">
                </span>

            </div>

            <div class="mb-3">

                <label asp-for="SchoolStandardId"
                       class="form-label fw-semibold">
                    School Standard
                </label>

                <select asp-for="SchoolStandardId"
                        asp-items="ViewBag.SchoolStandards"
                        class="form-select">

                    <option value="">
                        -- Select School Standard --
                    </option>

                </select>

                <span asp-validation-for="SchoolStandardId"
                      class="text-danger">
                </span>

            </div>

            <div class="mb-3">

                <label asp-for="PrerequisiteDesc"
                       class="form-label fw-semibold">
                </label>

                <textarea asp-for="PrerequisiteDesc"
                          rows="4"
                          class="form-control"></textarea>

            </div>

            <hr />

            <button class="btn btn-success">

                <i class="bi bi-check-circle me-1"></i>
                Save

            </button>

            <a asp-action="Index"
               class="btn btn-secondary">

                Cancel

            </a>

        </form>

    </div>

</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}