@model School

<div class="container mt-4">

    <div class="card shadow-lg p-4">

        <!-- IMAGE -->
        @if (!string.IsNullOrEmpty(Model.ImagePath))
        {
            <img src="@Model.ImagePath" class="img-fluid mb-3"
                 style="height:300px;object-fit:cover;width:100%;" />
        }

        <!-- NAME -->
        <h2 class="mb-3">@Model.Name</h2>

        <!-- RATING -->
        @if (ViewBag.AvgRating > 0)
        {
            <p>
                <strong>Rating:</strong>

                @{
                    int fullStars = (int)Math.Floor((double)ViewBag.AvgRating);
                }

                @for (int i = 0; i < fullStars; i++)
                {
                    <span style="color: gold;">★</span>
                }

                @for (int i = fullStars; i < 5; i++)
                {
                    <span style="color: lightgray;">★</span>
                }

                (@ViewBag.AvgRating.ToString("0.0"))
            </p>
        }

        <hr />

        <!-- BASIC INFO -->
        <div class="row">

            <div class="col-md-6">
                <p><strong>Email:</strong> @Model.EmailId</p>
                <p><strong>Contact:</strong> @Model.ContactNo</p>
                <p><strong>Established:</strong> @Model.EstablishmentDate.ToShortDateString()</p>
                <p><strong>Board:</strong> @Model.EducationBoard?.EducationBoardTitle</p>
                <p><strong>Medium:</strong> @Model.Medium?.MediumTitle</p>
            </div>

            <div class="col-md-6">
                <p><strong>Address:</strong> @Model.Address</p>
                <p><strong>Area:</strong> @Model.Area?.AreaName</p>
                <p><strong>City:</strong> @Model.Area?.City?.CityName</p>
                <p><strong>State:</strong> @Model.Area?.City?.State?.StateName</p>
                <p><strong>Country:</strong> @Model.Area?.City?.State?.Country?.CountryName</p>
            </div>

        </div>

        <hr />

        <!-- FACILITIES -->
        <h4>Facilities</h4>

        @if (Model.SchoolFacilities != null && Model.SchoolFacilities.Any())
        {
            @foreach (var sf in Model.SchoolFacilities)
            {
                <span class="badge bg-info me-2 mb-2">
                    @sf.Facility?.FacilityTitle
                </span>
            }
        }
        else
        {
            <p>No facilities listed</p>
        }

        <hr />

        <!-- ACHIEVEMENTS -->
        <h4>Achievements</h4>

        @if (Model.SchoolAchievements != null && Model.SchoolAchievements.Any())
        {
            <ul>
                @foreach (var a in Model.SchoolAchievements)
                {
                    <li>@a.Title</li>
                }
            </ul>
        }
        else
        {
            <p>No achievements added</p>
        }

        <hr />

        <!-- ADMISSION PROCESS -->
        <h4>Admission Process</h4>

        @if (Model.SchoolStandards != null && Model.SchoolStandards.Any())
        {
            foreach (var ss in Model.SchoolStandards)
            {
                <h5>@ss.Standard?.StandardTitle</h5>

                if (ss.AdmissionProcesses != null && ss.AdmissionProcesses.Any())
                {
                    <ol>
                        @foreach (var step in ss.AdmissionProcesses)
                        {
                            <li>
                                <strong>@step.ProcessTitle</strong> - @step.ProcessDesc
                            </li>
                        }
                    </ol>
                }
                else
                {
                    <p>No process defined</p>
                }
            }
        }

        <hr />

        <!-- PREREQUISITES -->
        <h4>Required Documents</h4>

        @if (Model.SchoolStandards != null && Model.SchoolStandards.Any())
        {
            foreach (var ss in Model.SchoolStandards)
            {
                <h5>@ss.Standard?.StandardTitle</h5>

                if (ss.AdmissionPrerequisites != null && ss.AdmissionPrerequisites.Any())
                {
                    <ul>
                        @foreach (var doc in ss.AdmissionPrerequisites)
                        {
                            <li>
                                <strong>@doc.PrerequisiteTitle</strong> - @doc.PrerequisiteDesc
                            </li>
                        }
                    </ul>
                }
                else
                {
                    <p>No prerequisites defined</p>
                }
            }
        }

        <hr />

        <!-- FEES -->
        <h4>Fees Structure</h4>

        @if (Model.SchoolStandards != null && Model.SchoolStandards.Any())
        {
            foreach (var ss in Model.SchoolStandards)
            {
                <h5>@ss.Standard?.StandardTitle</h5>

                if (ss.StandardFees != null && ss.StandardFees.Any())
                {
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Fee Type</th>
                                <th>Amount</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var fee in ss.StandardFees)
                            {
                                <tr>
                                    <td>@fee.FeeHead?.FeeHeadTitle</td>
                                    <td>₹ @fee.Amount</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p>No fees added</p>
                }
            }
        }

    </div>

    <hr />

    <!-- ADD REVIEW -->
    <h4>Add Review</h4>

    <form asp-action="AddReview" method="post">

        <input type="hidden" name="SchoolId" value="@Model.Id" />

        <div class="mb-2">
            <input type="text" name="UserName" class="form-control"
                   placeholder="Your Name" required />
        </div>

        <div class="mb-2">
            <select name="Rating" class="form-control">
                <option value="5">⭐⭐⭐⭐⭐</option>
                <option value="4">⭐⭐⭐⭐</option>
                <option value="3">⭐⭐⭐</option>
                <option value="2">⭐⭐</option>
                <option value="1">⭐</option>
            </select>
        </div>

        <div class="mb-2">
            <textarea name="Comment" class="form-control"
                      placeholder="Write your review"></textarea>
        </div>

        <button class="btn btn-success">Submit Review</button>

    </form>

    <hr />

    <!-- REVIEWS -->
    <h4>Reviews</h4>

    @if (Model.Reviews != null && Model.Reviews.Any())
    {
        foreach (var r in Model.Reviews)
        {
            <div class="card mb-2 p-2">
                <strong>@r.UserName</strong> - ⭐ @r.Rating
                <p>@r.Comment</p>
            </div>
        }
    }
    else
    {
        <p>No reviews yet</p>
    }

    <hr />

    <!-- APPLY -->
    <a asp-controller="Enquiry"
       asp-action="Create"
       asp-route-schoolId="@Model.Id"
       class="btn btn-primary">
        Apply Now
    </a>

</div>